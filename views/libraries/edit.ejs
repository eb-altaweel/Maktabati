<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/StyleSheet/Style.css" />
  <title>Edit Library</title>
</head>
<body>
  <div class="page-container">
    <h1 class="page-title">Edit Library</h1>

    <form action="/libraries/<%= library._id %>?_method=PUT" method="POST" enctype="multipart/form-data" class="library-form">
      <div class="form-group">
        <label for="name">Library Name:</label>
        <input type="text" id="name" name="name" value="<%= library.name %>" required />
      </div>

      <div class="form-group">
        <label for="location">Location:</label>
        <input type="text" id="location" name="location" value="<%= library.location %>" required />
      </div>

      <div class="form-group">
        <label for="address">Address:</label>
        <input type="text" id="address" name="address" value="<%= library.address %>" required />
      </div>

      <div class="form-group">
        <label for="description">Description:</label>
        <textarea id="description" name="description"><%= library.description %></textarea>
      </div>

      <div class="form-group checkbox-group">
        <label><input type="checkbox" name="hasSeating" <%= library.hasSeating ? 'checked' : '' %>> Has Seating</label>
        <label><input type="checkbox" name="hasStudyRoom" <%= library.hasStudyRoom ? 'checked' : '' %>> Has Study Room</label>
      </div>

      <div class="form-group time-group">
        <label>Open Time:</label>
        <input type="time" name="openTime" value="<%= library.openTime %>" required />
        <label>Close Time:</label>
        <input type="time" name="closeTime" value="<%= library.closeTime %>" required />
      </div>

      <div class="form-group">
        <label for="image">Image:</label>
        <input type="file" id="image" name="image" accept="image/*" />
      </div>

      <button type="submit" class="submit-btn">Update Library</button>
    </form>

    <div class="top-actions">
      <a href="/libraries" class="button back-btn">← Back to All Libraries</a>
    </div>
  </div>
</body>
</html>
